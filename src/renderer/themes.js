// Names of the built-in themes (must match the [data-theme="..."] blocks in themes.css).
// Someday these could be discovered from user .css files instead of hard-coded.
export const THEME_NAMES = {
  light: ['Light', 'GitHub', 'Solarized Light'],
  dark: ['Dark', 'One Dark', 'Dracula', 'Solarized Dark']
}
